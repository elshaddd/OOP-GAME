#ifndef ENUMS_H
#define ENUMS_H

enum Direction
{
    UP,
    DOWN,
    LEFT,
    RIGHT
};

enum Characteristics
{
    HEALTH,
    SCORE,
    DAMAGE
};

enum Status
{
    PAUSE,
    RUN,
    PASS,
    OVER
};
#endif